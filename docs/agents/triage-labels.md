# Triage labels (optional / future)

Default status vocabulary for future `/triage` enablement:

- `inbox`
- `ready`
- `blocked`
- `in-progress`
- `done`

This phase: `/triage` disabled; Demo intake uses local `.scratch` issues + SPEC.
